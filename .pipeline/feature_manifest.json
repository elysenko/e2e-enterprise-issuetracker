{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nBuild a full-stack team Issue Tracker as a single Node/Express + SQLite backend serving a React (Vite/TypeScript) SPA from one container on port 8080, replacing the current nginx static placeholder.\n\n## Scope\n### Files to modify\n- `Dockerfile` — replace nginx image with a multi-stage Node build (build SPA + server, run `node` on 8080).\n- `index.html` — replace placeholder markup with the Vite SPA entry (`<div id=\"root\">` + module script).\n- `nginx.conf` — delete (no longer used); its SPA-fallback role moves into Express.\n- `README.md` — document stack, dev/build/run, and the base-path deployment.\n\n### Files to create\n**Config / build**\n- `package.json` — deps + scripts (`dev`, `build` = vite build + tsc server, `start` = `node dist/server/index.js`).\n- `vite.config.ts` — `base: '/e2e-enterprise-issuetracker/'`, build `outDir: 'dist/client'`, dev proxy `/api`→localhost server.\n- `tsconfig.json` (client) + `tsconfig.server.json` (server, outDir `dist/server`).\n- `.dockerignore`, `.gitignore`.\n\n**Server (`server/`)**\n- `server/index.ts` — Express app: JSON body, mount `/api` router, `express.static('dist/client')`, SPA fallback `app.get('*', sendIndex)`, listen on `process.env.PORT || 8080`.\n- `server/db.ts` — `better-sqlite3` init at `/app/data/issuetracker.db`, run schema/migrations on boot.\n- `server/schema.sql` — tables: `users`, `projects`, `project_members`, `issues`, `comments`.\n- `server/auth.ts` — bcrypt hashing, `jsonwebtoken` sign/verify, `requireAuth` + `requireAdmin` middleware.\n- `server/routes/auth.ts`, `projects.ts`, `issues.ts`, `comments.ts`, `users.ts`, `health.ts`.\n\n**Client (`src/`)**\n- `src/main.tsx` — `<BrowserRouter basename=\"/e2e-enterprise-issuetracker\">`.\n- `src/App.tsx` — route table (below).\n- `src/api/client.ts` — fetch wrapper; `API_BASE = import.meta.env.BASE_URL.replace(/\\/$/,'') + '/api'`; attaches `Authorization: Bearer` from stored token; 401 → redirect to `/login`.\n- `src/auth/AuthContext.tsx` — token in `localStorage`, `me` state, login/signup/logout.\n- `src/components/RequireAuth.tsx`, `RequireAdmin.tsx` — route guards.\n- Pages: `LoginPage`, `SignupPage`, `DashboardPage`, `ProjectsPage`, `ProjectIssuesPage`, `IssueDetailPage`, `AdminProjectsPage`, `AdminUsersPage`.\n- Components: `NewIssueModal`, `AssignControl`, `StatusControl`, `CommentThread`, `AppLayout` (nav + logout).\n\n### Dependencies\n- Runtime: `express`, `better-sqlite3`, `jsonwebtoken`, `bcryptjs`, `zod` (input validation).\n- Frontend: `react`, `react-dom`, `react-router-dom`.\n- Dev: `typescript`, `vite`, `@vitejs/plugin-react`, `@types/*`, `tsx` (dev server), `concurrently`.\n\n### Integrations\nNone (per spec).\n\n### Assumptions\n- Stack chosen (React+Vite / Express / SQLite) because the repo is greenfield and the deploy target is a single container on port 8080 with no external DB or PVC.\n- SQLite (embedded); data is ephemeral across restarts — acceptable for staging.\n- `full_auth` model: first signup → ADMIN, subsequent → MEMBER; JWT (Bearer) auth. Public routes: `/login`, `/signup`, `/api/health`, `/api/health/deep`.\n- Priority enum `LOW|MEDIUM|HIGH` (default MEDIUM); status `OPEN|IN_PROGRESS|RESOLVED` (default OPEN).\n- Comments support one level of threading via nullable `parent_id`.\n- Base path `/e2e-enterprise-issuetracker` hardcoded (matches stable project/ingress name).\n- Assignee dropdown lists the project's members; admin adds members to projects.\n\n## Implementation Plan\n\n### Step 1: Scaffold build config\n**Files:** `package.json`, `vite.config.ts`, `tsconfig*.json`, `.gitignore`, `.dockerignore`\n**Change:** Set up Vite (base `/e2e-enterprise-issuetracker/`, outDir `dist/client`) and server tsc build. Scripts for dev (concurrent vite + tsx server) and prod build/start.\n\n### Step 2: Database layer\n**Files:** `server/db.ts`, `server/schema.sql`\n**Change:** Init SQLite at `/app/data/*.db` (create dir if missing), run schema idempotently. Schema:\n- `users(id, email UNIQUE, password_hash, name, role, created_at)`\n- `projects(id, name, description, created_by, created_at)`\n- `project_members(project_id, user_id, PRIMARY KEY(project_id,user_id))`\n- `issues(id, project_id, title, description, priority, status, assignee_id NULL, created_by, created_at, updated_at)`\n- `comments(id, issue_id, author_id, parent_id NULL, body, created_at)`\n\n### Step 3: Auth\n**Files:** `server/auth.ts`, `server/routes/auth.ts`\n**Change:** `POST /api/auth/signup` (first user → ADMIN else MEMBER, bcrypt hash, return JWT+user), `POST /api/auth/login`, `GET /api/auth/me`. `requireAuth` verifies Bearer JWT; `requireAdmin` checks role. Validate input with zod.\n\n### Step 4: Health endpoints\n**File:** `server/routes/health.ts`\n**Change:** `GET /api/health` → `{status:'ok'}`; `GET /api/health/deep` → runs a trivial SQLite query and reports DB status. Both public (mounted before auth). Served at root path so k8s probes and prefixed ingress both work.\n\n### Step 5: Projects API\n**File:** `server/routes/projects.ts`\n**Change:** `GET /api/projects` (ADMIN → all; MEMBER → joined via `project_members`); `POST /api/projects` (ADMIN); `GET /api/projects/:id` (membership-checked); `GET/POST /api/projects/:id/members` (ADMIN adds members).\n\n### Step 6: Issues API\n**File:** `server/routes/issues.ts`\n**Change:** `GET /api/projects/:id/issues?status=` (filter, project-scoped, membership-checked); `POST /api/projects/:id/issues` (defaults status OPEN); `GET /api/issues/:id`; `PATCH /api/issues/:id` (assign `assignee_id`, change `status`, edit fields; updates `updated_at`); `GET /api/dashboard/issues` (issues across caller's projects).\n\n### Step 7: Comments API\n**File:** `server/routes/comments.ts`\n**Change:** `GET /api/issues/:id/comments` (joined with author name, ordered, nested by `parent_id`); `POST /api/issues/:id/comments` (`{body, parentId?}`, records author + timestamp).\n\n### Step 8: Server entry + static serving\n**File:** `server/index.ts`\n**Change:** Mount health (public) + `/api` routers, then `express.static('dist/client')`, then `app.get('*', …)` returning `index.html` for SPA deep links (API 404s handled inside the api router). Listen on 8080.\n\n### Step 9: Client shell, auth, guards\n**Files:** `src/main.tsx`, `src/App.tsx`, `src/api/client.ts`, `src/auth/AuthContext.tsx`, `src/components/RequireAuth.tsx`, `RequireAdmin.tsx`, `AppLayout.tsx`\n**Change:** Router with `basename`; API client derives base from `BASE_URL` and injects Bearer token; guards redirect unauthenticated users to `/login`; layout with nav + logout (clears token).\n\n**Routes (each page root carries `data-flow`):**\n- `/login` → LoginPage *(public, flow: login)*\n- `/signup` → SignupPage *(public, flow: signup)*\n- `/` → redirect `/dashboard`\n- `/dashboard` → DashboardPage *(flow: dashboard)*\n- `/projects` → ProjectsPage *(flow: projects-list)*\n- `/projects/:projectId` → ProjectIssuesPage; `?status=` filter + `?modal=new-issue` *(flow: project-issues)*\n- `/projects/:projectId/issues/:issueId` → IssueDetailPage; `?modal=assign` *(flow: issue-detail)*\n- `/admin/projects` → AdminProjectsPage *(RequireAdmin, flow: admin-projects)*\n- `/admin/users` → AdminUsersPage *(RequireAdmin, flow: admin-users)*\n\n### Step 10: Pages & components\n**Files:** page + component files under `src/`\n**Change:** Build sign-in/sign-up forms; dashboard (issues across projects); project list → project issues with status filter (query param) and New Issue modal (query param); issue detail with assignee control, status control (Open/In Progress/Resolved), and threaded `CommentThread`; admin project + user (role) management. Each navigable state restores from its URL/query param.\n\n### Step 11: Dockerfile\n**File:** `Dockerfile`\n**Change:** Multi-stage. Builder `node:20-slim` + `apt-get install python3 make g++`, `npm ci`, `npm run build`. Runtime `node:20-slim`, copy `node_modules` (compiled better-sqlite3), `dist/`, `package.json`; `mkdir -p /app/data`; `ENV PORT=8080`; `EXPOSE 8080`; `CMD [\"node\",\"dist/server/index.js\"]`.\n\n### Step 12: Docs\n**File:** `README.md`\n**Change:** Document stack, local dev, build/run, base-path behavior, and ephemeral-data caveat.\n\n## Testing Strategy\n- Health: `GET /api/health` and `/api/health/deep` return ok (direct :8080 and via prefixed ingress).\n- Auth: first signup → ADMIN; second → MEMBER; login returns JWT; unauthenticated dashboard request → redirect to `/login`; guarded API returns 401 without token.\n- Issues: create → status OPEN and appears in list; assign → assignee name shown; PATCH status to RESOLVED → appears under resolved filter.\n- Comments: post → shows in thread with author name + timestamp; reply nests under parent.\n- Projects: member sees only joined projects; selecting a project filters its issues; admin can create projects/add members.\n- Routing: deep-link every route (incl. `?status=`, `?modal=`) after full reload through the ingress prefix — SPA fallback + `basename` resolve correctly and state restores.\n- Build: `docker build` compiles native module and image starts and serves on 8080.\n\n## Risks\n- **Ephemeral data:** no PVC → SQLite resets on pod restart; re-run signup to recreate admin. Documented.\n- **Base-path correctness:** asset/API/router paths all depend on the `/e2e-enterprise-issuetracker/` base; verify via a real reload through the ingress, not just localhost.\n- **Native module build:** `better-sqlite3` needs build tools in the builder stage; runtime copies the compiled `node_modules` (same base image) — keep stages on identical `node:20-slim`.\n- **Namespace ResourceQuota:** node uses more memory than nginx; if pods are rejected for missing requests, add modest `resources.requests/limits` to `k8s/deployment.yaml`.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {
    "_global": {
      "requirements": [
        {
          "text": "Revision: make the primary action button green, and add a project filter dropdown to the top of the issue list header.",
          "type": "behavioral",
          "source": "feedback",
          "rev": 1
        }
      ]
    }
  }
}